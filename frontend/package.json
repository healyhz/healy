{
  "name": "frontend",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "rsbuild --env-dir ../",
    "build": "rsbuild build"
  },
  "devDependencies": {
    "@rsbuild/core": "2.0.5"
  }
}
